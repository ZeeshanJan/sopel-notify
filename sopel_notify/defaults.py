# sopel_notify/defaults.py

DEFAULTS = {
    "watch_online": True,      # Notify when user joins network
    "watch_offline": True,     # Notify when user parts/quits
    "watch_nickchange": True,  # Notify when user changes nick
    "notify_channel": "#notify", # Channel where bot will send alerts
}